# macOS
.DS_Store

# Xcode build artifacts
DerivedData/
build/
*.xcuserdata

# Xcode per-user state (UI layout, breakpoints, scheme management)
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Workspaces (keep the project's bundled workspace)
*.xcworkspace
!*.xcodeproj/*.xcworkspace
